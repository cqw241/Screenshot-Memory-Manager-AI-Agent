# S01 项目脚手架 + 首启配置向导 + 验证门禁 · 工作记录

- 分支：`feature/s01-config-wizard`
- 开始日期：2026-07-21
- 负责人：Codex

## 验证环境边界

- 本机：DevEco Studio 6.1.1、HarmonyOS 6.1.1/API 24 SDK、Hvigor 6.24.3。
- 可用：本机构建、纯逻辑自动化/mock 测试、已部署的 HarmonyOS Emulator 镜像。
- 当前无用户自备厂商凭证，真实多模态成功、真实认证失败与纯文本模型拒绝均不得写成已验证。
- 当前无真实 HarmonyOS NEXT 设备，真机 HUKS、签名安装和交互体验均待验证。

## Task 进度

### Task 1.1 工程初始化 — [完成 2026-07-21]

- 实际做法（与计划的差异）：
  - 从 DevEco Studio 6.1.1 本机模板结构建立 Stage/ArkTS 工程；使用 API 24 SDK 编译，目标与兼容 API 均锁定 20。
  - 应用名为“拾光”，Bundle Name 为 `com.shiguang.memory`，主模块为 `entry`。
  - 签名信息不入库；实际模拟器/真机签名安装在设备验证阶段补充。
- 遇到的问题与解决：
  - 远端原为 SSH 地址，本机到 GitHub 22 端口被重置，已改为 HTTPS，后续使用现有 GitHub 登录凭据推送。
  - 规划阶段运行 Hvigor 产生了未跟踪 `.hvigor` 缓存，已通过 `.gitignore` 排除。
  - Hvigor 首次误用了 `C:\Program Files\nodejs`，安装内部 pnpm 失败；构建命令现显式设置 DevEco 内置 Node 与 `DEVECO_SDK_HOME`。
- 验证方式与结果：
  - `ohpm install`：通过。
  - API 24 SDK 下运行 `assembleHap`：通过，`BUILD SUCCESSFUL`。
  - 产物为未签名 HAP；签名、模拟器安装和真机启动尚未验证，不能宣称通过。
- 对应 commit：本 Task 提交。

### Task 1.2 配置模型与持久化 — [完成 2026-07-21]

- 实际做法（与计划的差异）：
  - 定义配置草稿、运行期配置、已验证元数据、加密信封与 `UNCONFIGURED / VERIFIED / CORRUPTED` 三态模型。
  - HUKS 使用不可导出的 AES-256 密钥和 AES-GCM/NoPadding；每次加密生成 12 字节随机 nonce，完整 HUKS 密文（含末尾 16 字节认证标签）以 Base64 存入 Preferences。
  - Preferences 使用 A/B 双槽和 `activeSlot` 指针；候选槽回读、解析并解密成功后才激活，激活失败时恢复旧指针。
  - `ConfigRepository` 只暴露非敏感元数据和受控临时解密回调，回调结束即清空持有的 Key 字符串引用。
- 遇到的问题与解决：
  - 当前工作区所在文件系统不支持 OHPM 创建依赖符号链接，Hypium 包下载完成后在 link 阶段报错；本机测试临时从 OHPM 缓存复制依赖，未把依赖目录加入版本库。换到 DevEco 默认支持的 NTFS 工作区后应重新执行 `ohpm install`。
- 验证方式与结果：
  - Hypium mock：首次保存与重启读取、密文落盘、加密失败保留旧槽、激活失败回滚、损坏 JSON 状态识别均通过。
  - `hvigorw test --mode module -p product=default`：通过；检查输出无测试 `ERROR`。
  - API 24 SDK 下 `assembleHap`：通过，生产 HUKS/Preferences 实现已参与 ArkTS 编译。
  - 尚未在模拟器或真机验证 HUKS 持久化；真机安全能力结果保留为待验证。
- 对应 commit：
  - 本 Task 提交。

### Task 1.3 配置向导 UI — [未开始]

- 实际做法（与计划的差异）：
- 遇到的问题与解决：
- 验证方式与结果：
- 对应 commit：

### Task 1.4 视觉验证门禁 — [未开始]

- 实际做法（与计划的差异）：
- 遇到的问题与解决：
- 验证方式与结果：
- 对应 commit：

### Task 1.5 启动路由 — [未开始]

- 实际做法（与计划的差异）：
- 遇到的问题与解决：
- 验证方式与结果：
- 对应 commit：
