# S02 核心曳光弹 · 工作记录

- 分支：`feature/s02-tracer-pipeline`
- 开始日期：2026-07-21
- 负责人：Codex

## 验证环境边界

- 本机：DevEco Studio 6.1.1、HarmonyOS 6.1.1 / API 24 SDK、Hvigor 6.24.3。
- 开发工作区：`C:\Dev\Screenshot-Memory-Manager-AI-Agent`；不使用 D 盘异常卷副本。
- Story 2 启动基线：Hypium 31/31 通过，API 24 `assembleHap` 通过。
- 当前可用 HarmonyOS Emulator；真实 HarmonyOS NEXT 真机仍待复验，模拟器结果不得表述为真机结果。
- 模型调用只使用设备内已验证配置，验证记录不得包含 API Key、图片 Base64、OCR 全文或模型原始回复。

## Task 进度

### Task 2.1 相册选图 + 端侧 OCR — [实现完成，真机验收待补]

- 实际做法（与计划的差异）：使用 `PhotoViewPicker` 单选图片；取消选择返回 `CANCELLED`。选中图片先通过只读文件描述符复制到 `filesDir/imports`，按识别到的 MIME 类型保留安全后缀，再从应用私有副本创建原始质量 `PixelMap` 并调用 Core Vision Kit `recognizeText()`。OCR 只统一换行并清理首尾空白；空结果或异常返回 `READY + RETRY_PENDING`。主页加入阶段性导入、处理中状态、私有副本信息和 OCR 预览，不含正式卡片。
- 发送确认：首次导入前按已验证配置的 `trustedOrigin` 说明 OCR 文本和缩略图的数据范围；确认记录存放于独立、非敏感 Preferences。服务源变化后必须重新确认。该步骤不读取或持有 API Key。
- 测试样例：在 `testdata/s02` 提交四张脚本确定性生成、无隐私的中文 PNG（讲座海报、取件码、DDL 通知、优惠券）及 `expected.json` 真值清单。
- 遇到的问题与解决：PhotoPicker 返回的媒体 URI 不能直接按普通路径复制；改为先 `fileIo.open()` 获取只读 fd，再由 fd 解码和复制。模拟器首次选择还会进入系统“仅可访问所选图片”确认页，这是系统隐私流程，不申请全相册读取权限。
- 自动验证：Hypium 36/36 通过；API 24 `assembleHap` 通过；`git diff --check` 通过。单元测试覆盖取消、成功、空 OCR、OCR 异常、文本规范化及资源释放。
- 模拟器验证：四张样例成功进入系统 PhotoPicker；讲座海报以 1080 × 1600、72158 bytes 完成应用私有复制。Core Vision 在当前 HarmonyOS Emulator 返回空文本，应用正确显示“待重试”且未崩溃。依既定口径，这不是 OCR 成功验收，四类 OCR 主要文字仍需 HarmonyOS NEXT 真机逐张复验。
- 对应 commit：本 Task 独立提交（见 Git 历史）。

### Task 2.2 LLM 理解客户端 — [实现完成，真实模型四样例通过]

- 实际做法（与计划的差异）：新增深理解模块，从 `filesDir/imports` 私有原图按比例生成 JPEG 85 缩略图并保存到独立的 `filesDir/thumbnails`；长边最大 1024 px，小图不放大。Base64 Data URL 只在缩略图生成器与模型请求内部传递，页面只接收缩略图 URI、宽高和编码字节数。主页现于单图导入和端侧 OCR 后继续发起理解，即使 OCR 为空也保留多模态图片理解，并在临时验收区展示类型、实体、动作与解析状态；正式卡片仍留待 Task 2.3–2.5。
- 请求与解析：调用必须经过 `ConfigRepository.withVerifiedConfig(...)`，并复用既有 URL 预检、可信源校验与 HTTP transport。Prompt 注入同一次捕获的设备本地时间、UTC 偏移和系统时区，要求保留 `timeText`、不得猜测缺失日期，并固定返回原始 JSON 对象。客户端精确校验对象字段、nullable string、类型/动作枚举及带时区且日历有效的 ISO-8601 `eventTime`；Markdown 围栏、语法错误、缺失/额外字段和错类型均为内容非法。首次非法使用同一 OCR、缩略图和时间上下文纠错一次，且不携带第一次原始回复；二次非法返回 `UNCATEGORIZED + 全 null 实体 + 空 actions + FALLBACK_INVALID_RESPONSE`。
- 安全与错误：每次请求对象的 Key 在 `finally` 清空，运行时 Key 再由 `withVerifiedConfig` 清空；Key、Base64、OCR 正文、完整请求体和模型原始回复不进入理解结果或日志。401/403、404/405、429、网络/超时、图片能力拒绝、其他 4xx/5xx 与外层响应不兼容分别返回脱敏分类错误，不生成未分类结果；未实现指数退避、后台重试或持久化队列。生产日志只记录缩略图宽高、编码字节数、请求次数、耗时与错误类别。
- 自动验证：Hypium 47/47 通过；覆盖横竖图缩放、小图不放大、缩略图请求、设备时间/时区注入、五种类型、严格非法矩阵、一次纠错、二次 fallback、错误分类和成功/异常/fallback 的 Key 清理。API 24 `assembleHap` 通过，`git diff --check` 通过；新增生产代码未引入编译警告，构建仍只有既有 HUKS、Preferences、OCR、PhotoPicker、私有原图关闭处理、`getContext` 弃用及未配置签名警告。
- 兼容策略与设备验证（2026-07-22 补充）：HarmonyOS Emulator 确认百炼基础地址会正确补全 `/chat/completions`；手动填写完整接口会被再次补全，不能作为 Base URL 使用。验证请求与正式理解请求均移除未纳入百炼 `image_url` 契约的 `detail` 字段。`enable_thinking: false` 改为按“官方端点 + 可切换思考模型”能力白名单注入：覆盖百炼各混合思考模型族、SiliconFlow 官方支持清单及腾讯 TokenHub 的 Qwen3.5 Plus/Flash；思考专用模型、未知模型和标准 OpenAI 端点不注入，火山方舟与智谱等使用不同 `thinking` 契约的服务也不误发该字段。完整 Hypium 回归 68/68、API 24 `assembleHap` 与 `git diff --check` 均通过。
- 真实模型单样例验收（2026-07-22）：经用户明确授权，在 Emulator 使用设备内既有百炼配置 `qwen3.7-plus-2026-05-26` 导入无隐私取件码样例；应用私有原图 1080 × 1440、55340 bytes，发送缩略图 768 × 1024、40529 bytes、JPEG 85，在既定 15 秒窗口内返回 `PICKUP_CODE / PARSED` 并保存卡片 `1f5af400-848c-441a-bbf3-9cedd5a19ec9`，本地数量为 1。发现模型将截图中缺失年份的“7月23日前”推导为 2026 年事件时间，违反“不得猜测缺失日期组件”的语义要求；该风险已在 Task 2.5 通过客户端日期证据校验修复，并完成四样例复验。验收后仍须轮换曾暴露的 Key。
- 对应 commit：本 Task 独立提交（见 Git 历史）。

### Task 2.3 卡片存储层 — [实现完成，设备重启验收通过]

- 实际做法：新增卡片深模块，页面只通过 `create/getById/updateUnderstanding/deleteById/listTimeline` 接口访问卡片，不接触 SQL、JSON 编解码和数据库版本。卡片 ID 复用应用私有原图 ID，持久化私有原图 URI、缩略图 URI、类型、精确实体 JSON、动作 JSON、解析状态、创建时间及事件时间索引；不存 OCR 正文、Base64、API Key、模型原始回复或完整请求体。
- RelationalStore：生产适配器使用加密的 `shiguang_cards.db`、`SecurityLevel.S3` 和数据库版本 1，以缓存 Promise 串行完成首次打开及建表。表约束类型和 `PARSED/FALLBACK_INVALID_RESPONSE` 状态，复合索引按事件时间、创建时间和 ID 倒序；无事件时间的卡片排在有事件时间卡片之后。初始化失败回滚，所有 ResultSet 均在 `finally` 关闭，生产适配器未新增 ArkTS 编译警告。
- 数据完整性与删除：事件时间保留原始带时区 ISO 文本，并转换为 epoch 毫秒用于跨时区正确排序；读取时重新校验枚举、精确实体结构、动作及事件时间/索引一致性，损坏或存储异常仅返回脱敏分类错误。更新只替换结构化理解和事件时间索引，不改变 ID、图片 URI 或创建时间；删除只删数据库行，不调用文件接口，因此不会删除相册源图、应用私有原图或缩略图。
- 链路接入：主页在理解成功或二次非法响应的安全 fallback 后立即创建卡片；认证、网络或服务错误没有理解结果，因此不落库。进入主页时读取已有卡片数量和最近保存 ID，临时验收区展示本地持久化摘要；落库失败不抹掉已经完成的 OCR、缩略图和结构化理解预览。正式时间轴、详情页和删卡 UI 仍留给 Task 2.4。
- 自动验证：Hypium 58/58 通过，其中新增 11 个卡片测试，覆盖创建/读取/更新/删除、重复与缺失 ID、nullable/fallback、跨时区 epoch、稳定时间轴排序、非法 JSON/枚举/动作/时间/索引、适配器错误脱敏及无文件删除副作用。API 24 `assembleHap` 通过，`git diff --check` 通过；构建只保留既有 HUKS、Preferences、OCR、PhotoPicker、私有原图关闭处理、`getContext` 弃用和未配置签名警告。
- 设备验证（2026-07-22 补验）：HarmonyOS Emulator 恢复在线后发现重复导入会在已存在的 `filesDir/imports` 上再次执行 `fileIo.mkdir()`，触发 `13900015 File exists`；`filesDir/thumbnails` 存在同类隐患。修复为共享的幂等私有目录初始化，仅在目标确为目录时接受 `File exists`，其他错误仍原样抛出；新增 3 项回归测试。覆盖安装后连续导入均新增应用私有原图及对应 JPEG 缩略图。模型请求恢复后，真实取件码样例成功落库为 `PICKUP_CODE / PARSED`；随后强制停止并重启应用，本地数量仍为 1，最近 ID 仍为 `1f5af400-848c-441a-bbf3-9cedd5a19ec9`，确认加密 RelationalStore 持久化一致。当前完整 Hypium 回归为 68/68。
- 对应 commit：本 Task 独立提交（见 Git 历史）。

### Task 2.4 时间轴与详情 UI — [实现完成，模拟器验收通过]

- 实际做法：主页改为“时间轴优先”，以紧凑导入入口、加载/空态/读取失败状态和按事件日期分组的卡片列表替换 Task 2.1–2.3 临时验收面板。新增纯展示模块，将五类截图稳定映射为中文徽标与标题，按设备时区生成分组日期，保持仓储排序且不猜测缺失实体；无事件时间卡片统一进入“未识别事件时间”。保存成功、主页首次进入及从详情返回时都会重新读取时间轴，递增加载版本避免旧请求覆盖新结果。
- 详情与删除：新增独立 `pages/CardDetail` 路由，首页只传 `cardId`，详情通过仓储重新读取最新卡片；使用私有原图 URI 展示完整比例图片，并展示类型、标题、导入时间、时间表达、原始 ISO 事件时间、地点、码号、金额、建议动作和解析状态。路由参数无效、卡片不存在、读取失败及图片解码失败均有安全降级。删除前明确二次确认，只调用 `deleteById`；失败保留详情并可重试，成功返回后首页刷新，不删除任何图片文件。
- 自动验证：采用公开展示 seam 完成 red→green TDD，新增 6 项 Hypium 测试，覆盖五种标题/徽标、关键实体与动作、fallback、无时间卡片、导入时间、设备时区边界和输入顺序保持。完整 Hypium 74/74 通过；API 24 `assembleHap` 通过；`git diff --check` 通过。生产构建未为 Task 2.4 新增 ArkTS 警告，仍只有既有 HUKS、Preferences、OCR、PhotoPicker、私有原图关闭处理、`Index.getContext` 弃用和未配置签名警告。
- 模拟器验证：在 API 24 Pura 90 Emulator 覆盖安装并保留 Task 2.3 的一张无隐私取件码卡片；时间轴正确显示 1 张卡片、7 月 23 日分组、私有缩略图、取件码标题及时间/地点/码号/动作。点击进入独立详情后，私有原图与全部字段正确；取消删除后仍为 1 张，确认删除后返回“0 张”空态，强制停止并重启后空态保持。删除后对应 `1f5af400-848c-441a-bbf3-9cedd5a19ec9` 的私有 PNG 与 JPEG 仍存在，确认删除仅影响数据库记录。
- 对应 commit：本 Task 独立提交（见 Git 历史）。

### Task 2.5 完整链路联调 — [代码与模拟器联调完成，真机 OCR 待验]

- 链路编排：新增轻量 `ScreenshotIngestionCoordinator`，通过导入、理解和卡片写入三个窄接口串联单图摄取，并统一返回取消、成功及脱敏的导入/理解/存储失败结果。协调器拒绝并发导入，页面只消费“等待选图 → 私有复制与 OCR → 内容理解 → 保存卡片”阶段；保存成功后由页面刷新时间轴。
- 交互与计时：`ScreenshotImportService` 在 PhotoPicker 确认返回后发出生命周期事件，首页从该时刻计时，到卡片落库且时间轴刷新可见结束。按钮处理期间禁用并显示旋转进度，阶段文案覆盖 OCR 降级、理解、保存和刷新；成功提示显示一位小数耗时，超过 15 秒只警告性能未达标，不丢弃已保存卡片。
- 日期证据规则：Prompt 和客户端同时要求 `eventTime` 非空时，`timeText` 必须明确包含年、月、日、时、分，并与 ISO 时间组件一致；支持中文日期和 `YYYY-M-D` / `YYYY/M/D` 常见形式。原文无秒时只允许省略秒或 `00` 秒；缺失组件、组件冲突或凭空补秒均视为非法响应，沿用一次纠错和二次 fallback。完整 `timeText` 仍允许 `eventTime = null`。
- 自动验证：按公开 seam 完成 red→green TDD，新增协调器成功、取消、OCR 降级、理解失败、fallback 落库、存储失败和并发防重测试，并覆盖日期缺年份、缺日期、缺时分、组件冲突、补秒、数字日期及 nullable 时间。完整 Hypium 86/86、API 24 `assembleHap` 与 `git diff --check` 均通过；生产构建未新增 ArkTS 警告。
- 模拟器四样例（2026-07-22）：使用设备内既有已验证模型配置逐张导入 `testdata/s02`。讲座海报 `POSTER` 用时 4.0 秒，时间为 2026-07-25 14:00、地点为图书馆报告厅；取件码 `PICKUP_CODE` 用时 3.0 秒，码号 A8B6、地点南门快递站，原文“7月23日前”保留且 `eventTime = null`，因此进入“未识别事件时间”分组；DDL 通知 `DDL_NOTICE` 用时 3.2 秒，时间为 2026-07-28 23:59、地点为教学平台；优惠券 `COUPON` 用时 3.6 秒，时间为 2026-07-31 23:59、地点为学生活动中心一层、金额为满30减10元。四张均有私有缩略图、正确时间轴摘要和详情字段，处理期间 UI 可响应。
- 持久化与环境边界：四张样例完成后强制停止并重启 `com.shiguang.memory`，时间轴仍为 4 张且排序、实体与缩略图完整。当前 Emulator 的 Core Vision 对四张样例均返回空文本，应用按设计继续多模态图片理解；这只证明 OCR 降级链路，不替代真机 OCR 主要文字验收。Task 2.1 的真机项、Task 2.5 的“Story 验收标准全部通过”和 Story 2 总验收继续待补。
- 对应 commit：本 Task 独立提交（见 Git 历史）。

## Story 2 → Story 3 交接 — [完成 2026-07-22]

- 新增 `docs/handover/S02-交接文档.md`，汇总零上下文构建、自动验证、四样例模拟器验收、关键模块、已知限制和 Story 3 接入建议。
- 当前状态固定为“Task 2.5 代码及模拟器联调完成，真机 OCR 待验”。Story 2 总验收、Task 2.1 真机 OCR 和 Task 2.5“Story 验收标准全部通过”保持未勾选。
- 后续暂以 Pura 90 HarmonyOS 6.1.1（API 24）模拟器开发 Story 3。Story 3 不得把真机 OCR 当作已验证能力；依赖 OCR 质量的产品判断须通过接口抽象和确定性模拟数据推进，真机 OCR 只作为后续补验项。
- Story 2 以带上述验收边界的 PR 进入评审；在真机 OCR 补验完成前，不宣称 Story 2 全部验收通过。
