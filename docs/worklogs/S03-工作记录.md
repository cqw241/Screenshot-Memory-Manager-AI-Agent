# S03 截图自动摄取 · 工作记录

- 分支：`feature/s03-silent-ingest`
- 开始日期：2026-07-22
- 负责人：Codex

## 验证边界

- 本机：DevEco Studio 6.1.1、HarmonyOS 6.1.1 / API 24 SDK、Hvigor 6.24.3。
- 当前唯一设备：DevEco Pura 90 配置的 HarmonyOS Emulator；设备属性报告 `emulator 6.1.0.125(SP9DEVC00E120R4P11)`、API 24。
- 当前安装包不是正常可交付签名：`appProvisionType=debug`、`appPrivilegeLevel=normal`、`appDistributionType=none`、`appSignType=none`、`isSystemApp=false`、`isPreInstallApp=false`；命令行构建也明确跳过签名。
- 未修改 APL，未使用特殊调试签名，签名材料未进入仓库。以下结果全部是模拟器预检，不能用于判定 L0/L1/L2。
- 探针只保存时间戳、生命周期、回调/媒体类型、大小与时间字段，以及 URI、identifier、相册 URI、文件名的 SHA-256 摘要；不保存图片内容、OCR 全文、完整媒体 URI、API Key 或模型响应。

## Task 3.0 平台能力探针 — [探针实现完成，正式定级阻塞]

### 可交付探针

- 新增诊断页面及服务，使用 API 20+ `READ_IMAGEVIDEO`、`photoChange`、最近媒体查询、`PhotoSource.SCREENSHOT`、通知和 WantAgent 接口；没有接入生产自动摄取、后台消费者或通知 UI。
- `EntryAbility` 记录 `ON_CREATE / ON_FOREGROUND / ON_BACKGROUND / ON_NEW_WANT / ON_DESTROY`；事件以最多 200 条的脱敏 Preferences 日志持久化，写入串行化，损坏 JSON 安全回退为空。
- 通知探针固定测试 `cardId=probe-card-s03-001`；页面同时显示实际收到的原始 `cardId`，便于证明 Want 参数没有丢失。
- 自动判定器只有在正常可交付签名证据齐全时才返回 L0/L1/L2；模拟器、未签名或矩阵不完整时保持 `UNKNOWN`。

### 2026-07-22 模拟器预检

权限与媒体接口：

- `READ_IMAGEVIDEO` 检查结果为 `status=-1; granted=false`。
- 申请操作能正常拉起系统“允许拾光读取你的图片和视频？”授权对话框；本轮未代替用户选择隐私授权，因此没有把对话框出现等同于授权成功。
- 未授权状态启动 `photoChange` 返回 `errorCode=UNAVAILABLE`；监听没有注册，无法测量回调延迟。
- `getRecentPhotoInfo(PhotoSource.SCREENSHOT)` 返回 `errorCode=UNAVAILABLE`。

媒体矩阵：探针的四类记录入口各执行 3 次，但本轮没有在系统授权后生成对应真实媒体，12 次查询均返回 `latestAsset=EMPTY`。该结果只证明空库/未授权路径不崩溃，不构成截图字段筛选证据。

| 类型 | 第 1 次 | 第 2 次 | 第 3 次 | 预检结论 |
| --- | --- | --- | --- | --- |
| 截图 | `EMPTY` | `EMPTY` | `EMPTY` | 未取得真实样本 |
| 拍照 | `EMPTY` | `EMPTY` | `EMPTY` | 未取得真实样本 |
| 下载图片 | `EMPTY` | `EMPTY` | `EMPTY` | 未取得真实样本 |
| 普通保存图片 | `EMPTY` | `EMPTY` | `EMPTY` | 未取得真实样本 |

生命周期矩阵：

| 状态 | 模拟器表现 | 回调结论 |
| --- | --- | --- |
| 前台 | 页面正常打开并记录 `ON_CREATE / ON_FOREGROUND`；首次无参数进入曾暴露空参数崩溃，修复后覆盖安装复验通过 | `photoChange` 未获准注册，不能统计 |
| 后台驻留 | Home 后进程 PID `10749` 仍存在，重新启动 Ability 可回前台 | `photoChange` 未获准注册，不能统计 |
| 模拟系统回收 | 对 PID 执行 `kill -9` 被设备拒绝：`Operation not permitted` | 本环境无法模拟，不作结论 |
| 用户强制停止 | `aa force-stop com.shiguang.memory` 成功，随后不会自行唤醒；显式启动恢复正常 | 强停期间不宣称有回调；重启后 22 条脱敏探针记录和 Story 2 的 4 张卡片均保留 |

通知与深链：

- 未授权直接发布通知返回 `errorCode=1600004`；通知权限申请按钮已实现，但本轮未代替用户操作系统隐私授权。
- 因通知未发布，不能宣称通知点击携带 `cardId` 的 WantAgent 已通过。
- 作为独立参数链路预检，`aa start ... --ps cardId probe-card-s03-direct` 能触发 `onNewWant`，探针页原样显示 `probe-card-s03-direct`。这只验证 Want 参数解析与页面回传，不替代通知点击验收。

### 当前能力结论与阻塞

- **最终能力等级：`UNKNOWN / 未定级`。** 当前证据不是正常可交付签名和真机证据，媒体矩阵也没有真实样本，禁止据此判定 L0、L1 或 L2。
- 正式关闭 Task 3.0 仍需一台真实 HarmonyOS NEXT 设备和正常可交付证书/Profile，重新执行授权、4 × 3 媒体样本、四类生命周期、通知发布及 WantAgent 点击矩阵。
- 截图规则只有在截图 3/3 命中、其余媒体 0/9 误判、重查和重启后稳定时才可采用；优先系统来源/相册标识，文件名不能单独作为依据。
- 若普通签名在构建、签名、安装或授权任一步拒绝 `READ_IMAGEVIDEO`，必须保存原始错误码、移除不可交付权限声明并以 L0 收尾；特殊签名结果不能升级结论。

### 自动验证与阶段交付

- 在 Story 2 的 86 项基线上新增 7 项 Hypium，覆盖未签名证据保持 `UNKNOWN`、L0/L1/L2 判定、媒体身份字段脱敏、生命周期日志/损坏 JSON 和 Want `cardId` 边界；完整测试共 93 项。
- API 24 `assembleHap` 与 `git diff --check` 通过；生产构建没有为探针新增 ArkTS 警告，仍保留 Story 2 既有警告和“未配置 signingConfigs”警告。
- 模拟器覆盖安装后探针页面可打开，Story 2 时间轴仍显示原有 4 张卡片；强制停止并重启后卡片与探针记录均保持。
- 本提交仅为 `S03-T0: 搭建平台能力探针`，不得标记 Task 3.0 正式完成。真机结论必须另行执行完整检查后提交 `S03-T0: 记录真机能力结论`。

## Task 3.1 自动摄取基础设施 — [门禁未满足，未启动]

- Task 3.1 只在 Task 3.0 的正常可交付签名真机结论为 L1 或 L2 时实施。
- 当前等级为 `UNKNOWN`，因此本阶段没有创建 `sourceMediaKey`、数据库 v2、任务表、状态机、已知 URI 导入入口或自动摄取消费者，也没有改动 Story 2 手动导入语义。
- 若最终为 L0，将在本记录明确标注“Task 3.1 不适用”，只保留 Story 2 手动导入。
